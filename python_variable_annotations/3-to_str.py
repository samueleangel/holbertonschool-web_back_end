#!/usr/bin/env python3
"""
This script handles a function that converts a float
into a string with the same value represented
"""


def to_str(n: float) -> str:
    """
    This function will convert a float into a string,
    with the same value represented, just changing it´s
    data type.

    Args:
        n (float): Float data type

    Returns:
        n converted to string: "n"
    """
    return str(n)
